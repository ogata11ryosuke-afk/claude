# Local Claude Code overrides (contains credential paths/secrets)
.claude/settings.local.json

# Google service account keys — NEVER commit
service-account*.json
*-service-account.json
gcp-key*.json
credentials.json

# General
.DS_Store
*.log

# Testing framework: project-local specs/wireframes (kept on user's local machine)
testing-framework/projects/*/specs/
testing-framework/projects/*/specs-local/
# Excel temp/lock files
~$*.xlsx
*.xlsx#
